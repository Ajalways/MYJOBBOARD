{
  "name": "proofjobs-root",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "install": "cd src && npm install",
    "build": "cd src && npm run build",
    "start": "cd src && npm run start:deploy",
    "deploy": "cd src && npm install && npm run build"
  },
  "engines": {
    "node": "18.x",
    "npm": ">=8.0.0"
  }
}
